require "love.timer"








local channel = love.thread.getChannel("Tempory")

love.timer.sleep(2)

print("on attend")

--local client = channel:demand()
print("on en a un")
--local myNumber = channel:demand()
print("on en a 2")

--OpenConnect(client, myNumber)

while true do
    
end